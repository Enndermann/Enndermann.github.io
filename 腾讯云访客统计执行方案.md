# 腾讯云访客信息统计执行方案

## 1. 目标与范围

目标是在当前 Astro 博客中接入一套自有访客统计链路，将访客 IP、访问时间、访问页面等信息通过腾讯云云函数写入腾讯云数据库。

本方案只规划实现步骤，不直接修改现有源码。当前项目中已经存在统计入口：

- 前端统计组件：`src/components/Analytics.astro`
- 全站挂载位置：`src/layouts/Layout.astro` 中的 `<Analytics />`
- 当前统计端点：`https://blog-analytics.enderman2021.workers.dev/collect`

推荐将现有 Cloudflare Worker 统计端点迁移为腾讯云 CloudBase HTTP 访问服务 + 云函数 + CloudBase 数据库。原因是腾讯云传统 SCF API 网关触发器相关文档已提示 API 网关产品计划于 2025-06-30 停止服务，不建议新方案继续依赖旧式 API 网关触发器。

## 2. 推荐架构

```text
访客浏览器
  -> src/components/Analytics.astro
  -> CloudBase HTTP 访问服务 /api/visit
  -> CloudBase 云函数 blog-visit-track
  -> CloudBase 数据库集合 blog_visits
```

核心设计：

- 前端只上报页面上下文，不上报 IP。
- 云函数从请求上下文或代理头中读取客户端 IP。
- 访问时间以服务端时间为准，避免信任客户端时间。
- 数据库禁止客户端直接写入，只允许云函数服务端写入。
- 默认保存 `ipHash`，是否保存明文 `ip` 由隐私策略决定。

## 3. 数据采集字段

### 3.1 前端上报字段

前端允许上报：

```json
{
  "site": "blog",
  "url": "https://example.com/posts/demo/",
  "path": "/posts/demo/",
  "title": "文章标题",
  "referrer": "https://example.com/",
  "language": "zh-CN",
  "timezone": "Asia/Shanghai",
  "screen": {
    "width": 1920,
    "height": 1080
  }
}
```

前端禁止上报：

- 数据库密钥
- 云函数密钥
- 明文 IP
- 管理员凭证
- 可直接操作数据库的 token

### 3.2 服务端补充字段

云函数写库时补充：

```json
{
  "ip": "203.0.113.10",
  "ipHash": "sha256(ip + salt)",
  "userAgent": "Mozilla/5.0 ...",
  "visitedAt": "2026-07-07T12:00:00.000Z",
  "createdAt": "2026-07-07T12:00:00.000Z",
  "requestId": "cloud-function-request-id"
}
```

建议生产环境默认只使用 `ipHash` 做统计和去重。若确实需要明文 `ip` 用于安全排查，应在博客隐私说明中明确告知，并设置数据保留周期。

## 4. 数据库设计

### 4.1 集合名称

```text
blog_visits
```

### 4.2 文档结构

```json
{
  "_id": "auto",
  "site": "blog",
  "url": "https://example.com/posts/demo/",
  "path": "/posts/demo/",
  "title": "文章标题",
  "referrer": "https://example.com/",
  "language": "zh-CN",
  "timezone": "Asia/Shanghai",
  "screen": {
    "width": 1920,
    "height": 1080
  },
  "ip": "203.0.113.10",
  "ipHash": "sha256",
  "userAgent": "Mozilla/5.0 ...",
  "visitedAt": "Date",
  "createdAt": "Date",
  "requestId": "string"
}
```

### 4.3 索引建议

```text
visitedAt desc
site asc, visitedAt desc
path asc, visitedAt desc
ipHash asc, visitedAt desc
```

用途：

- `visitedAt desc`：查看最新访问记录。
- `site + visitedAt`：未来支持多个站点共用同一套统计服务。
- `path + visitedAt`：统计文章访问量。
- `ipHash + visitedAt`：按访客去重、排查异常请求。

### 4.4 数据保留策略

建议：

- 原始访问明细保留 90-180 天。
- 聚合统计结果长期保留，例如每日 PV、UV、热门文章。
- 如果保存明文 IP，建议保留周期短于原始明细，例如 30-90 天。

## 5. 腾讯云侧配置步骤

### 5.1 创建 CloudBase 环境

1. 进入腾讯云控制台。
2. 创建 CloudBase 云开发环境。
3. 记录环境 ID，例如：

```text
TCB_ENV_ID=your-env-id
```

### 5.2 创建数据库集合

1. 在 CloudBase 数据库中新建集合：

```text
blog_visits
```

2. 设置集合权限：

```text
客户端不可读写
仅云函数服务端读写
```

3. 创建索引：

```text
visitedAt desc
site asc + visitedAt desc
path asc + visitedAt desc
ipHash asc + visitedAt desc
```

### 5.3 创建云函数

函数名称：

```text
blog-visit-track
```

运行时建议：

```text
Node.js 18 或 Node.js 20
```

环境变量：

```text
TCB_ENV_ID=your-env-id
IP_HASH_SALT=一段随机长字符串
ALLOWED_ORIGINS=https://你的博客域名
SAVE_RAW_IP=false
```

说明：

- `IP_HASH_SALT` 必须只保存在云函数环境变量中，不能写入前端代码。
- `ALLOWED_ORIGINS` 用英文逗号分隔，未来可支持预览域名和正式域名。
- `SAVE_RAW_IP=false` 表示默认不保存明文 IP。

### 5.4 配置 HTTP 访问服务

将 HTTP 路径映射到云函数：

```text
/api/visit -> blog-visit-track
```

绑定完成后得到公开访问地址：

```text
https://你的-cloudbase-http-域名/api/visit
```

### 5.5 配置 CORS

允许来源：

```text
https://你的博客域名
```

允许方法：

```text
POST, OPTIONS
```

允许请求头：

```text
Content-Type
```

不建议允许：

```text
*
```

## 6. 云函数实现方案

### 6.1 依赖

云函数目录建议包含：

```text
package.json
index.js
```

依赖：

```json
{
  "dependencies": {
    "@cloudbase/node-sdk": "^2.0.0",
    "zod": "^3.23.8"
  }
}
```

部署前需要以腾讯云控制台或 CloudBase CLI 支持的方式安装依赖。

### 6.2 云函数职责

云函数必须完成以下工作：

1. 处理 `OPTIONS` 预检请求。
2. 只允许 `POST` 写入。
3. 校验 `Origin` 是否在白名单。
4. 限制请求体大小。
5. 使用 Schema 校验请求体。
6. 从可信请求上下文中提取 IP。
7. 生成 `ipHash`。
8. 写入 `blog_visits` 集合。
9. 返回 `204 No Content`。
10. 对业务错误返回明确状态码，不返回底层堆栈。

### 6.3 云函数参考实现

```js
const cloudbase = require("@cloudbase/node-sdk");
const crypto = require("crypto");
const { z } = require("zod");

const app = cloudbase.init({
  env: process.env.TCB_ENV_ID
});

const db = app.database();

const VisitSchema = z.object({
  site: z.string().min(1).max(64),
  url: z.string().url().max(2048),
  path: z.string().min(1).max(512),
  title: z.string().max(256).optional().default(""),
  referrer: z.string().max(2048).optional().default(""),
  language: z.string().max(64).optional().default(""),
  timezone: z.string().max(128).optional().default(""),
  screen: z
    .object({
      width: z.number().int().positive().max(10000),
      height: z.number().int().positive().max(10000)
    })
    .optional()
});

function buildCorsHeaders(origin) {
  return {
    "Access-Control-Allow-Origin": origin,
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type",
    "Access-Control-Max-Age": "86400"
  };
}

function parseAllowedOrigins() {
  return (process.env.ALLOWED_ORIGINS || "")
    .split(",")
    .map(item => item.trim())
    .filter(Boolean);
}

function getHeader(headers, name) {
  const target = name.toLowerCase();
  const key = Object.keys(headers || {}).find(item => item.toLowerCase() === target);
  return key ? headers[key] : "";
}

function getClientIp(event) {
  const headers = event.headers || {};
  const forwardedFor = getHeader(headers, "x-forwarded-for");
  if (forwardedFor) {
    return forwardedFor.split(",")[0].trim();
  }

  return (
    getHeader(headers, "x-real-ip") ||
    event.requestContext?.sourceIp ||
    event.requestContext?.http?.sourceIp ||
    ""
  );
}

function hashIp(ip) {
  const salt = process.env.IP_HASH_SALT;
  if (!salt) {
    throw new Error("IP_HASH_SALT is required");
  }

  return crypto.createHash("sha256").update(`${ip}:${salt}`).digest("hex");
}

function parseBody(event) {
  const rawBody = event.isBase64Encoded
    ? Buffer.from(event.body || "", "base64").toString("utf8")
    : event.body || "{}";

  if (Buffer.byteLength(rawBody, "utf8") > 8192) {
    const error = new Error("Request body is too large");
    error.statusCode = 413;
    throw error;
  }

  return JSON.parse(rawBody);
}

exports.main = async (event, context) => {
  const headers = event.headers || {};
  const origin = getHeader(headers, "origin");
  const allowedOrigins = parseAllowedOrigins();
  const corsHeaders = allowedOrigins.includes(origin) ? buildCorsHeaders(origin) : {};
  const method = event.httpMethod || event.method || event.requestContext?.http?.method || "";

  if (method === "OPTIONS") {
    return {
      statusCode: allowedOrigins.includes(origin) ? 204 : 403,
      headers: corsHeaders,
      body: ""
    };
  }

  if (!allowedOrigins.includes(origin)) {
    return {
      statusCode: 403,
      headers: corsHeaders,
      body: JSON.stringify({ message: "Forbidden origin" })
    };
  }

  if (method !== "POST") {
    return {
      statusCode: 405,
      headers: corsHeaders,
      body: JSON.stringify({ message: "Method not allowed" })
    };
  }

  try {
    const input = VisitSchema.parse(parseBody(event));
    const now = new Date();
    const ip = getClientIp(event);
    const saveRawIp = process.env.SAVE_RAW_IP === "true";

    const record = {
      ...input,
      ipHash: hashIp(ip),
      userAgent: getHeader(headers, "user-agent"),
      visitedAt: now,
      createdAt: now,
      requestId: context.requestId || ""
    };

    if (saveRawIp) {
      record.ip = ip;
    }

    await db.collection("blog_visits").add(record);

    return {
      statusCode: 204,
      headers: corsHeaders,
      body: ""
    };
  } catch (error) {
    const statusCode = error.statusCode || (error.name === "ZodError" ? 400 : 500);
    const message = statusCode === 500 ? "Internal server error" : error.message;

    return {
      statusCode,
      headers: corsHeaders,
      body: JSON.stringify({ message })
    };
  }
};
```

## 7. 当前博客代码接入方案

### 7.1 修改位置

只需要修改现有文件：

```text
src/components/Analytics.astro
```

`src/layouts/Layout.astro` 已经挂载 `<Analytics />`，无需新增挂载逻辑。

### 7.2 前端脚本目标形态

将当前 endpoint：

```ts
const endpoint = "https://blog-analytics.enderman2021.workers.dev/collect";
```

替换为腾讯云 HTTP 访问服务地址：

```ts
const endpoint = "https://你的-cloudbase-http-域名/api/visit";
```

同时建议将上报内容改为结构化对象：

```astro
---
const endpoint = "https://你的-cloudbase-http-域名/api/visit";
---

<script is:inline define:vars={{ endpoint }}>
  (function () {
    if (navigator.doNotTrack === "1") return;

    var data = {
      site: "blog",
      url: window.location.href,
      path: window.location.pathname,
      title: document.title,
      referrer: document.referrer || "",
      language: navigator.language || "",
      timezone: "",
      screen: {
        width: window.screen.width,
        height: window.screen.height
      }
    };

    try {
      if (window.Intl && Intl.DateTimeFormat) {
        data.timezone = Intl.DateTimeFormat().resolvedOptions().timeZone || "";
      }
    } catch (e) {}

    function send() {
      try {
        var payload = JSON.stringify(data);

        if (navigator.sendBeacon) {
          var blob = new Blob([payload], {
            type: "application/json"
          });
          navigator.sendBeacon(endpoint, blob);
          return;
        }

        fetch(endpoint, {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: payload,
          keepalive: true,
          credentials: "omit"
        }).catch(function () {});
      } catch (e) {}
    }

    if (document.visibilityState === "prerender") {
      document.addEventListener(
        "visibilitychange",
        function () {
          if (document.visibilityState === "visible") send();
        },
        { once: true }
      );
    } else {
      send();
    }
  })();
</script>
```

### 7.3 Astro 页面切换注意点

当前项目启用了：

```astro
<ClientRouter />
```

如果站内导航使用 Astro View Transitions，传统立即执行脚本可能只在首次页面加载时触发。正式实现时需要验证站内跳转是否触发统计。如果不触发，应改为监听：

```js
document.addEventListener("astro:page-load", send);
```

建议最终实现时将发送逻辑改为：

```text
首次打开页面 -> 发送一次
Astro 客户端路由完成 -> 再发送对应页面路径
```

并用 `lastTrackedUrl` 避免同一个 URL 重复上报。

## 8. 安全策略

### 8.1 Origin 白名单

云函数必须校验 `Origin`：

```text
https://你的博客域名
```

本地调试时可以临时加入：

```text
http://localhost:4321
```

上线后删除本地来源。

### 8.2 请求方法限制

只允许：

```text
POST
OPTIONS
```

拒绝：

```text
GET
PUT
PATCH
DELETE
```

### 8.3 请求体限制

建议最大请求体：

```text
8 KB
```

超过限制返回：

```text
413 Payload Too Large
```

### 8.4 Schema 校验

所有输入字段必须经过 Schema 校验：

- 字符串长度限制
- URL 格式限制
- 屏幕尺寸范围限制
- 可选字段默认值
- 拒绝未知大型字段

### 8.5 IP 处理

推荐策略：

```text
保存 ipHash
默认不保存明文 ip
需要安全审计时再开启 SAVE_RAW_IP=true
```

`ipHash` 生成方式：

```text
sha256(ip + ":" + IP_HASH_SALT)
```

### 8.6 限流策略

基础版本可以先依赖云函数和数据库写入限制。增强版本建议增加：

```text
同一 ipHash 10 秒内同一路径最多写入 1 次
同一 ipHash 1 分钟内最多写入 30 次
异常 userAgent 可拒绝写入
```

限流状态可存入单独集合：

```text
blog_visit_rate_limits
```

## 9. 验证流程

### 9.1 云函数接口验证

使用真实博客域名 Origin 测试：

```bash
curl -i -X POST "https://你的-cloudbase-http-域名/api/visit" \
  -H "Origin: https://你的博客域名" \
  -H "Content-Type: application/json" \
  -d '{"site":"blog","path":"/","url":"https://你的博客域名/","title":"home","referrer":"","language":"zh-CN","timezone":"Asia/Shanghai","screen":{"width":1920,"height":1080}}'
```

期望：

```text
HTTP 204
数据库 blog_visits 出现一条记录
visitedAt 为服务端时间
ipHash 存在
```

### 9.2 非法来源验证

```bash
curl -i -X POST "https://你的-cloudbase-http-域名/api/visit" \
  -H "Origin: https://evil.example.com" \
  -H "Content-Type: application/json" \
  -d '{"site":"blog","path":"/","url":"https://你的博客域名/"}'
```

期望：

```text
HTTP 403
数据库无新增记录
```

### 9.3 非法数据验证

```bash
curl -i -X POST "https://你的-cloudbase-http-域名/api/visit" \
  -H "Origin: https://你的博客域名" \
  -H "Content-Type: application/json" \
  -d '{"site":"blog","path":"/","url":"not-a-url"}'
```

期望：

```text
HTTP 400
数据库无新增记录
```

### 9.4 本地构建验证

修改 `src/components/Analytics.astro` 后执行：

```bash
pnpm lint
pnpm build
```

期望：

```text
lint 通过
astro check 通过
astro build 通过
dist 产物正常生成
```

### 9.5 浏览器端验证

1. 启动本地开发服务：

```bash
pnpm dev
```

2. 打开首页和任意文章页。
3. 在浏览器 DevTools Network 中查看 `/api/visit` 请求。
4. 验证首次加载、站内跳转、刷新页面是否都能正确上报。
5. 验证开启浏览器 Do Not Track 时不会上报。

## 10. 分阶段 TODO

### 阶段 1：腾讯云资源准备

- [ ] 创建 CloudBase 环境。
- [ ] 创建 `blog_visits` 数据库集合。
- [ ] 创建必要索引。
- [ ] 设置数据库集合为仅云函数可写。
- [ ] 创建云函数 `blog-visit-track`。
- [ ] 配置环境变量 `TCB_ENV_ID`、`IP_HASH_SALT`、`ALLOWED_ORIGINS`、`SAVE_RAW_IP`。
- [ ] 配置 HTTP 访问服务路径 `/api/visit`。

### 阶段 2：云函数开发与验证

- [ ] 添加 CloudBase SDK 和 Zod 依赖。
- [ ] 实现 CORS 处理。
- [ ] 实现 Origin 白名单。
- [ ] 实现请求体大小限制。
- [ ] 实现 Schema 校验。
- [ ] 实现 IP 提取和 `ipHash` 生成。
- [ ] 实现数据库写入。
- [ ] 使用 `curl` 验证成功写入。
- [ ] 使用非法 Origin 验证拒绝写入。
- [ ] 使用非法 URL 验证 Schema 拒绝。

### 阶段 3：博客前端接入

- [ ] 备份当前 `src/components/Analytics.astro` 的统计端点。
- [ ] 将 endpoint 替换为腾讯云 HTTP 访问服务地址。
- [ ] 调整 payload 为结构化字段。
- [ ] 验证 `sendBeacon` 上报。
- [ ] 验证 `fetch keepalive` 兜底。
- [ ] 验证 Astro 站内跳转是否触发统计。
- [ ] 如站内跳转未触发，增加 `astro:page-load` 监听。

### 阶段 4：上线与观测

- [ ] 执行 `pnpm lint`。
- [ ] 执行 `pnpm build`。
- [ ] 部署博客。
- [ ] 访问首页和文章页。
- [ ] 检查 CloudBase 函数日志。
- [ ] 检查 `blog_visits` 数据库写入。
- [ ] 检查异常请求比例。
- [ ] 检查是否存在重复上报。

### 阶段 5：合规与维护

- [ ] 在博客隐私说明中补充访客统计说明。
- [ ] 明确记录字段、用途、保留周期。
- [ ] 配置定期清理原始访问明细。
- [ ] 如保留明文 IP，确认 `SAVE_RAW_IP=true` 的必要性。
- [ ] 定期检查云函数调用量和数据库存储量。

## 11. 回滚方案

如果腾讯云统计链路异常，可以快速回滚：

1. 将 `src/components/Analytics.astro` 中的 endpoint 改回原 Cloudflare Worker 地址。
2. 重新执行：

```bash
pnpm lint
pnpm build
```

3. 重新部署博客。
4. 暂停或下线腾讯云 HTTP 访问服务，避免继续产生费用。

如果只是不想继续记录访客信息，也可以将 `Analytics.astro` 中的 `<script>` 内容临时清空，保留组件文件，避免影响 `Layout.astro` 的导入。

## 12. 风险与注意事项

- IP 地址属于敏感访问信息，应避免长期保存明文 IP。
- 前端传来的任何字段都不可信，必须由云函数做 Schema 校验。
- `Origin` 校验不能替代完整的滥用防护，但可以阻挡大多数跨站滥用。
- `sendBeacon` 的响应不可被前端读取，所以调试阶段需要结合 CloudBase 日志和数据库验证。
- Astro View Transitions 可能导致站内切换时脚本不重复执行，必须单独验证。
- 如果博客部署在多个域名下，所有正式域名都需要加入 `ALLOWED_ORIGINS`。
- 如果未来要做后台统计看板，建议新增聚合集合，不要直接在前端暴露原始访问明细。

## 13. 官方资料入口

- 腾讯云云函数 SCF 文档：`https://cloud.tencent.com/document/product/583`
- 腾讯云 API 网关触发器说明：`https://cloud.tencent.com/document/product/583/12513`
- 腾讯云 CloudBase 云开发文档：`https://cloud.tencent.com/document/product/876`

